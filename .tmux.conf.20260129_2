# Tmux settings
#-------------------------------------------------------------------------------------------------------------------
#
#		Reload tmux from inside: Ctrl+a & : source-file ~/.tmux.conf
#		or from shell tmux source-file ~/.tmux.conf
#
#		updates for 1.9x
#		remove mouse stuff
#-------------------------------------------------------------------------------------------------------------------
#
unbind % # Remove default binding since we're replacing


#-------------------------------------------------------------------------------------------------------------------
#
#		Reload config
#
#-------------------------------------------------------------------------------------------------------------------
#
bind r source-file ~/.tmux.conf \; display-message "  Config reloaded..."

#-------------------------------------------------------------------------------------------------------------------
# Get rid of the annoying bell, might not work in all terminals
#-------------------------------------------------------------------------------------------------------------------
#
set-option -g default-terminal "screen-256color"
set -g default-terminal "screen-256color"
set -g history-limit 5000
set-option -g prefix C-a

#-------------------------------------------------------------------------------------------------------------------
# Count sessions start at 1
# Start window numbering at 1
#-------------------------------------------------------------------------------------------------------------------
#
set-option -g base-index 1
set -g base-index 1
set -g pane-base-index 1

#-------------------------------------------------------------------------------------------------------------------
# open new windows in the current path
#-------------------------------------------------------------------------------------------------------------------
#
bind c new-window -c "#{pane_current_path}"

#-------------------------------------------------------------------------------------------------------------------
#		Windows/Panes
#			Automatically set window title
#-------------------------------------------------------------------------------------------------------------------
#
setw -g automatic-rename on
set -g renumber-windows on
set-window-option -g automatic-rename on
set-option -g set-titles on

bind-key C-s last-window
bind , command-prompt "rename-window '%%'"
bind-key X confirm-before -p "kill-window #W? (y/n)" kill-window
bind c new-window
if "[[ `tmux -V` == *1.9* ]]" 'unbind c; bind c new-window -c "#{pane_current_path}"'

#-------------------------------------------------------------------------------------------------------------------
#	key for last-window
#-------------------------------------------------------------------------------------------------------------------
#
bind-key C-a last-window
bind-key a send-key C-a

#-------------------------------------------------------------------------------------------------------------------
#		Split windows
#-------------------------------------------------------------------------------------------------------------------
#
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

bind-key e setw synchronize-panes
set -g history-limit 10000

#-------------------------------------------------------------------------------------------------------------------
#		Bind d to detach session
#-------------------------------------------------------------------------------------------------------------------
bind-key d detach

#-------------------------------------------------------------------------------------------------------------------
#		Set window notifications
#-------------------------------------------------------------------------------------------------------------------
#
set -g monitor-activity on
set -g visual-activity on

#-------------------------------------------------------------------------------------------------------------------
#		Highlight active window
#-----------------------------------------------------------------------------
#
#set-window-option -g window-status-current-bg red

#set-window-option -g window-style 'bg=#181818'
#set-window-option -g window-active-style 'bg=black'
#set-window-option -g pane-active-border-style ''

#-------------------------------------------------------------------------------------------------------------------
#		Clock
#-------------------------------------------------------------------------------------------------------------------
#
set-window-option -g clock-mode-colour colour64 #green

#-------------------------------------------------------------------------------------------------------------------
# Url browsing
#-------------------------------------------------------------------------------------------------------------------
#
bind u capture-pane \; save-buffer /tmp/tmux-buffer \; new-window -n "urlview" '$SHELL -c "urlview < /tmp/tmux-buffer"'


#-------------------------------------------------------------------------------------------------------------------
#
#		THEME - Make it look nice
#
#		Date format: hostname weekday month day, hour:minute
#-------------------------------------------------------------------------------------------------------------------
#		Set the title bar
#
set -g set-titles on
set -g set-titles-string ' #{prefix_highlight} #(whoami) :: #h :: #(curl ipecho.net/plain;echo)'

#-------------------------------------------------------------------------------------------------------------------
##	Status bar all
#-------------------------------------------------------------------------------------------------------------------
#
# set-option -g status-utf8 on
set -g status-justify left
set -g status-bg black
set -g status-fg white
set -g window-status-current-bg white
set -g window-status-current-fg black
set -g window-status-current-attr bold
set -g status-interval 15

#-------------------------------------------------------------------------------------------------------------------
#		Left pane
#-------------------------------------------------------------------------------------------------------------------
#
set -g status-left-length 90
set -g status-left "  #{prefix_highlight} | #[fg=Green]#(whoami)#[fg=white]::#[fg=grey]#(hostname -s)#[fg=white]::#[fg=yellow]#(curl ipecho.net/plain;echo)"

#-------------------------------------------------------------------------------------------------------------------
#		Right pane
#-------------------------------------------------------------------------------------------------------------------
#
# set -g status-right 'Online: #{online_status }#[fg=white]%d-%m-%Y %H:%M:%S#[default]  '
# set -g status-right "Online: #{online_status} | \
	#{battery_status_bg} Batt: #{battery_icon} |#[default] %a %h-%d %H:%M "

# set -g status-right "#{prefix_highlight}"



###		If no icons show
set -g @online_icon "ok"
set -g @offline_icon "offline!"
set -g @batt_charged_icon "ï¿½ï¿½"
set -g @batt_charging_icon "ï¿½ï¿½"
set -g @batt_attached_icon "ï¿½ï¿½"
set -g @batt_full_charge_icon "ï¿½ï¿½ "
set -g @batt_high_charge_icon "ï¿½ï¿½ "
set -g @batt_medium_charge_icon "ï¿½ï¿½ "
set -g @batt_low_charge_icon "ï¿½ï¿½ "

#-------------------------------------------------------------------------------------------------------------------
#		Legend
#-------------------------------------------------------------------------------------------------------------------
#
#   H   Host,
#   I   Current window index
#   P   Current pane index
#   S   Session name
#   T   Current window title
#   W   Current window name
#   #   A literal .#.

#-------------------------------------------------------------------------------------------------------------------
#
#   Panelswitching
#
#-------------------------------------------------------------------------------------------------------------------
#	Keyboard panel switching
#-------------------------------------------------------------------------------------------------------------------
#
#   switch panes using Alt-arrow without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

#-------------------------------------------------------------------------------------------------------------------
#	Mouse panel switching
#-------------------------------------------------------------------------------------------------------------------
#
#set -g mouse-utf8 on
set -g mouse on

#-------------------------------------------------------------------------------------------------------------------
#	Tmux defaults to line drawing? (not on synology/busybox linux)
#-------------------------------------------------------------------------------------------------------------------
#?
set-option -ga terminal-overrides ',*:enacs@:smacs@:rmacs@:acsc@'

#-------------------------------------------------------------------------------------------------------------------
#	Open a man page in new window
#-------------------------------------------------------------------------------------------------------------------
#
bind m command-prompt "split-window 'exec man %%'"
 
#-------------------------------------------------------------------------------------------------------------------
#   Rename your terminals
#-------------------------------------------------------------------------------------------------------------------
#
set -g set-titles on
set -g set-titles-string '#(whoami)::#h::#(curl ipecho.net/plain;echo)'

#-------------------------------------------------------------------------------------------------------------------
#	Fix for setting icons for online plugin
#-------------------------------------------------------------------------------------------------------------------
#set -g @online_icon "ok"
#set -g @offline_icon "offline!"

###	###########################################################################
#
#	Reference sites
#	http://www.rushiagr.com/blog/2016/06/16/everything-you-need-to-know-about-tmux-copy-pasting-ubuntu/i
#	https://github.com/hamvocke/dotfiles
#
#	Plugins
#	https://github.com/tmux-plugins/tpm
#	https://github.com/tmux-plugins/tmux-continuum
#	https://github.com/tmux-plugins/tmux-resurrect
#	https://github.com/tmux-plugins/tmux-battery
#   https://github.com/tmux-plugins/tmux-prefix-highlight

#-------------------------------------------------------------------------------------------------------------------
#	Plugins
#-------------------------------------------------------------------------------------------------------------------
#
#-------------------------------------------------------------------------------------------------------------------
#
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/vim-tmux'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
#set -g @plugin 'plugins/tmux-resurrect'
#set -g @plugin 'plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-online-status'
set -g @plugin 'tmux-plugins/tmux-sidebar'
set -g @pnet__speed_interfaces "enp0s25"
set -g @continuum-restore 'on'
set -g @continuum-boot 'on'

#
#	Resurect
#
# run '~/.tmux/plugins/resurrect.tmux'
# run '~/.tmux/plugins/continuum.tmux'
#run '~/.tmux/plugins/tpm/tpm'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm'"
run '~/.tmux/plugins/tpm/tpm'

