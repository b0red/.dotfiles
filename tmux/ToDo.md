# Things to do / fix for .tmux.conf

 - [ ] If all apps installed, from the is_it_installed(), then it should just report "All apps installed; (<appname(s)>)" instead of listing them all again.
  - [ ] Check for variable set from ~/dotfiles/RunMe.sh. If it's then skip this script and exit gracefully.
  - [ ] Set this check in RunMe.sh so it runs before anything else.

## tmux-menus
1) Skapa en meny för olika bash script som kan startas enkelt från den menyn...
    ev 2 olika, en för Docker containers och en för "vanliga script"
    
