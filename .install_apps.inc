#!/usr/bin/env bash
# .install_apps.inc: List of apps to install (edit here, no script changes needed)
# Format: APP_ARRAY=(app1 app2 ...)
APP_ARRAY=(
    curl
    htop
    btop
    ncdu
    pydf
    tree
    tmux
    vim
    mc
    fd-find
    git
    bat
    # Add more: ripgrep fzf neovim zsh etc.
)